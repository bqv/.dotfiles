# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout somewhen, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see http://i3wm.org/docs/userguide.html for a complete reference!

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Control+q exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

# mod key
set $mod Mod4
# workspace names
set $1 "1: Shell"
set $2 "2: Web"
set $3 "3: Media"
set $4 "4: Work"
set $5 "5: Dev"
set $6 "6: Games"
set $7 "7: ?"
set $8 "8: Chat"
set $9 "9: Updates"
set $0 "~"

assign [class="XTerm" instance="pacman"] $9
assign [class="Python2"] $8
assign [class="Skype"] $8
assign [class="URxvt" instance="irc"] $8
assign [class="Steam"] $6
assign [class="Eclipse"] $5
assign [class="Emacs"] $4
assign [class="mplayer2"] $3
assign [class="mpv"] $3
assign [class="Dwb"] $2
assign [class="luakit"] $2
assign [class="Firefox"] $2
assign [class="Chromium"] $2
assign [class="URxvt"] $1

for_window [class="URxvt"] border 1pixel
for_window [class="Chromium"] border pixel 2
for_window [class="Emacs"] border none

workspace_layout stacking

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below. ISO 10646 = Unicode
font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, if you need a lot of unicode glyphs or
# right-to-left text rendering, you should instead use pango for rendering and
# chose a FreeType font, such as:
# font pango:DejaVu Sans Mono 10

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec urxvt
# start a browser
bindsym $mod+b exec chromium
bindsym $mod+Control+b exec dwb
bindsym $mod+Shift+b exec i3-input -F 'exec chromium %s' -P 'URL: '
bindsym $mod+Control+Shift+b exec i3-input -F 'exec dwb %s' -P 'URL: '
# start another browser
bindsym $mod+Shift+f exec firefox
# start a file manager
bindsym $mod+Control+f exec pcmanfm
# start an editor
bindsym $mod+Control+e exec emacsclient -c
bindsym $mod+g exec gummi
# start a command
bindsym $mod+Shift+x exec i3-input -F 'exec xterm -e sh -c "%s&&read||read"' -P 'sh# '
# start irc
bindsym $mod+i exec urxvt -name irc -e irc
# start skype
bindsym $mod+Shift+i exec skype

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+p exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
bindsym $mod+m exec --no-startup-id i3-dmenu-desktop

# take a screenshot
bindsym $mod+F12 exec xwd -root -display :0.0 | convert xwd:- .screenshots/$(date -Ins).png
# take a windowshot
bindsym $mod+F11 exec xwd -display :0.0 | convert xwd:- .screenshots/$(date -Ins).png

# run an ipc command
bindsym $mod+c exec i3run

# rename a workspace
bindsym $mod+n exec i3-input -F 'rename workspace to "%s"' -P 'New tag name: '

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move focused window
bindsym $mod+Shift+h move container left
bindsym $mod+Shift+j move container down
bindsym $mod+Shift+k move container up
bindsym $mod+Shift+l move container right

# split in horizontal orientation
bindsym $mod+s split h

# split in vertical orientation
bindsym $mod+z split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+q layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+d focus child

# switch to workspace
bindsym $mod+1 workspace $1
bindsym $mod+2 workspace $2
bindsym $mod+3 workspace $3
bindsym $mod+4 workspace $4
bindsym $mod+5 workspace $5
bindsym $mod+6 workspace $6
bindsym $mod+7 workspace $7
bindsym $mod+8 workspace $8
bindsym $mod+9 workspace $9
bindsym $mod+0 workspace $0
# switch to last workspace
bindsym $mod+Tab workspace back_and_forth
# switch to prev workspace
bindsym $mod+Left workspace prev
# switch to next workspace
bindsym $mod+Right workspace next

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $1
bindsym $mod+Shift+2 move container to workspace $2
bindsym $mod+Shift+3 move container to workspace $3
bindsym $mod+Shift+4 move container to workspace $4
bindsym $mod+Shift+5 move container to workspace $5
bindsym $mod+Shift+6 move container to workspace $6
bindsym $mod+Shift+7 move container to workspace $7
bindsym $mod+Shift+8 move container to workspace $8
bindsym $mod+Shift+9 move container to workspace $9
bindsym $mod+Shift+0 move container to workspace $0
# move focused container to last workspace
bindsym $mod+Shift+Tab move container to workspace back_and_forth
# move focused container to prev workspace
bindsym $mod+Shift+Right move container to workspace prev
# move focused container to next workspace
bindsym $mod+Shift+Left move container to workspace next

# Make the currently focused window a scratchpad
bindsym $mod+Shift+minus move scratchpad

# Show the first scratchpad window
bindsym $mod+minus scratchpad show

# starting a specially named term automatically 
# move this to scratchpad, if active
#exec --no-startup-id emacs -name scratchpad
#for_window [class="Emacs" instance="scratchpad"] move scratchpad
exec --no-startup-id gummi
for_window [class="Gummi"] move scratchpad

bindsym $mod+Up move workspace to VGA1
bindsym $mod+Down move workspace to LVDS1

workspace $1 output LVDS1
workspace $2 output LVDS1
#workspace $3 output ?
workspace $4 output LVDS1
workspace $5 output VGA1
#workspace $6 output ?
#workspace $7 output ?
workspace $8 output LVDS1
workspace $9 output LVDS1

# resize window (you can also use the mouse for that)
mode "move/resize" {
        # These bindings trigger as soon as you enter the move/resize mode

        # grow or shrink the window’s width/height
        bindsym Shift+h resize shrink width 10 px or 10 ppt
        bindsym Shift+j resize grow height 10 px or 10 ppt
        bindsym Shift+k resize shrink height 10 px or 10 ppt
        bindsym Shift+l resize grow width 10 px or 10 ppt

        # same keys, but for the motion
        bindsym h move left
        bindsym j move down
        bindsym k move up
        bindsym l move right

        # same keys, but for the reverse growth
        bindsym Control+h resize grow width 10 px or 10 ppt; move left
        bindsym Control+j resize shrink height 10 px or 10 ppt; move down
        bindsym Control+k resize grow height 10 px or 10 ppt; move up
        bindsym Control+l resize shrink width 10 px or 10 ppt; move right

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}
bindsym $mod+r mode "move/resize"

# run autostart commands
exec_always --no-startup-id compton --config ~/.comptonrc -b
exec_always --no-startup-id sh -c "source ~/bin/ttor"
exec_always --no-startup-id xrdb -merge ~/.Xresources
exec_always --no-startup-id xrdb -merge ~/.Xdefaults
exec_always --no-startup-id feh --bg-scale ~/.wallpaper ~/.background
exec_always --no-startup-id xset dpms 0 0 1500
exec_always --no-startup-id xset m 64 0
exec --no-startup-id x11vnc -autoport 8644 -passwd onyx -auth guess -forever -oa /var/log/vnc/x11vnc.log
exec --no-startup-id redshiftgui
exec --no-startup-id dropboxd
exec --no-startup-id udiskie
exec --no-startup-id fcitx
exec --no-startup-id nm-applet
exec --no-startup-id setxkbmap gb -option compose:ralt,keypad:pointerkeys,ctrl:nocaps
exec --no-startup-id twmnd
exec --no-startup-id unclutter
exec --no-startup-id clipit -d
exec --no-startup-id conky -c info.conkyrc
exec --no-startup-id conky -c sys.conkyrc
exec --no-startup-id conky -c logs.conkyrc
exec --no-startup-id conky -c network.conkyrc
exec --no-startup-id conky -c weather.conkyrc
exec --no-startup-id conky -c fortune.conkyrc

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
        id bar-0
        output LVDS1
        status_command i3status
        position bottom
        tray_output LVDS1
}

bar {
        id bar-1
        output LVDS1
        status_command sh -c "conky -c .conky/status.conkyrc | dzen2 -h 18 -y 0 -bg '#050505' -fn 'Verdana:bold:size=9' -title-name Conky -xs 1"
        position top
        workspace_buttons no
        tray_output none
}

bar {
        id bar-2
        output VGA1
        position top
        workspace_buttons yes
        tray_output none
}

# Volume mods
set $amixer amixer -D pulse sset Master
set $twmnc twmnc --fg grey -t Volume --id 99
set $volm /usr/share/icons/gnome/16x16/status/audio-volume-high.png
set $mute /usr/share/icons/gnome/16x16/status/audio-volume-muted.png
bindsym XF86AudioRaiseVolume exec $amixer 5%+ | awk '/^  Front Left/{if($$6=="[off]"){print "-i $mute -c -MUTE-\\ "$$5}else{print "-i $volm -c "$$5} }' | tr -d "[]" | xargs $twmnc {}
bindsym XF86AudioLowerVolume exec $amixer 5%- | awk '/^  Front Left/{if($$6=="[off]"){print "-i $mute -c -MUTE-\\ "$$5}else{print "-i $volm -c "$$5} }' | tr -d "[]" | xargs $twmnc {}
bindsym XF86AudioMute exec $amixer toggle     | awk '/^  Front Left/{if($$6=="[off]"){print "-i $mute -c -MUTE-\\ "$$5}else{print "-i $volm -c "$$5} }' | tr -d "[]" | xargs $twmnc {}
bindsym Mod4+Mod1+1 exec $amixer 10%;    exec $twmnc -i $volm -c "Set to 10%"
bindsym Mod4+Mod1+2 exec $amixer 20%;    exec $twmnc -i $volm -c "Set to 20%"
bindsym Mod4+Mod1+3 exec $amixer 30%;    exec $twmnc -i $volm -c "Set to 30%"
bindsym Mod4+Mod1+4 exec $amixer 40%;    exec $twmnc -i $volm -c "Set to 40%"
bindsym Mod4+Mod1+5 exec $amixer 50%;    exec $twmnc -i $volm -c "Set to 50%"
bindsym Mod4+Mod1+6 exec $amixer 60%;    exec $twmnc -i $volm -c "Set to 60%"
bindsym Mod4+Mod1+7 exec $amixer 70%;    exec $twmnc -i $volm -c "Set to 70%"
bindsym Mod4+Mod1+8 exec $amixer 80%;    exec $twmnc -i $volm -c "Set to 80%"
bindsym Mod4+Mod1+9 exec $amixer 90%;    exec $twmnc -i $volm -c "Set to 90%"
bindsym Mod4+Mod1+0 exec $amixer toggle; exec $twmnc -i $mute -c "(un-)muted"
bindsym Mod4+Mod1+grave exec pavucontrol

# Fn keys
bindsym XF86Sleep exec systemctl suspend; exec xset dpms force off
bindsym XF86HomePage exec chromium
bindsym XF86ScreenSaver exec i3lock -d -c 000000 -p win -i ~/.lock
bindsym Pause exec xset dpms force off
