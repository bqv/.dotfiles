[user]
        name = Tony Olagbaiye
        email = tony.olagbaiye@answerdigital.com
[push]
        default = simple
[pull]
        rebase = true
[commit]
        gpgsign = false
[credential]
        helper = store

[filter "spacify"]
        clean = expand --tabs=4 --initial
        requirwd
[filter "trim"]
        clean = git-stripspace
        required
[filter "spacify_trim"]
        clean = sh -c "expand --tabs=4 --initial %f | git-stripspace"
        required
